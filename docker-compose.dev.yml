services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: shg-api-dev
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env.development
